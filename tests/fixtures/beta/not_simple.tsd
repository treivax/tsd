// Test négation simple
type Person(#id: string, name: string, active: bool)


// Action definitions
action active_person(arg1: string)

rule r1 : {p: Person} / NOT (p.active == false) ==> active_person(p.id)

Person(id:P001, name:Alice, active:true)
Person(id:P002, name:Bob, active:false)
