# Exemple d'utilisation de l'instruction reset
# Cette instruction permet de réinitialiser complètement le système

# Définir un premier type
type User(name: string, age: number)

# Définir un second type
type Order(id: number, amount: number)

# Utiliser l'instruction reset pour tout effacer
reset

# Après reset, on peut redéfinir de nouveaux types
type Product(id: string, name: string, price: number)

type Category(name: string, priority: number)

# Tous les types, règles et faits précédents ont été supprimés
# Le système est maintenant vide et prêt pour de nouvelles définitions
