// Test opérateurs de comparaison dans jointures beta
type User : <id: string, name: string, score: number, created: number>
type Activity : <id: string, user_id: string, points: number, timestamp: number>

{u: User, a: Activity} / u.id == a.user_id AND a.points > u.score ==> improvement_activity(u.id, a.id)
{u: User, a: Activity} / u.id == a.user_id AND a.timestamp >= u.created ==> valid_activity(u.id, a.id)
{u: User, a: Activity} / u.id == a.user_id AND a.points <= 50 ==> low_activity(u.id, a.id)
{u: User, a: Activity} / u.id == a.user_id AND a.points != u.score ==> different_score_activity(u.id, a.id)
